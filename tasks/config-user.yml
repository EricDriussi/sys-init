---
- name: Create Docker Group
  group:
    name: docker
    state: present

- name: Ensure Docker User
  user:
    name: "{{ user }}"
    groups:
      - docker
    state: present
    shell: /bin/bash

- name: Create ssh dir
  file:
    path: ~/.ssh/id_rsa
    state: directory
    mode: '0755'

- name: Create ssh keypair
  openssh_keypair:
    path: ~/.ssh/id_rsa
    owner: "{{ user }}"
    type: rsa
    size: 4096
    state: present
    passphrase: "{{ ssh_passphrase }}"
