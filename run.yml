---
- name: CONFIG USER
  tags: user
  hosts: localhost
  connection: local
  tasks:
    - import_tasks: tasks/config-user.yml

- name: INSTALL ESSENTIALS
  tags: install
  hosts: ansible
  tasks:
    - import_tasks: tasks/essentials.yml
