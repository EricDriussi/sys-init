---
- name: CONFIG USER
  tags: user
  hosts: localhost
  tasks:
    - import_tasks: tasks/config-user.yml

- name: INSTALL ESSENTIALS
  tags: install
  hosts: localhost
  tasks:
    - import_tasks: tasks/essentials.yml
